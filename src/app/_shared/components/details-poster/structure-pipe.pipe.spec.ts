import { StructurePipePipe } from './structure-pipe.pipe';

describe('StructurePipePipe', () => {
  it('create an instance', () => {
    const pipe = new StructurePipePipe();
    expect(pipe).toBeTruthy();
  });
});
