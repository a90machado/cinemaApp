import { ScheduleDayPipe } from './schedule-day.pipe';

describe('ScheduleDayPipe', () => {
  it('create an instance', () => {
    const pipe = new ScheduleDayPipe();
    expect(pipe).toBeTruthy();
  });
});
